/**
 * Shared utilities and types used across the application.
 *
 * @example
 * import { clamp, lerp } from '@shared';
 * import type { Position, Size, GameState } from '@shared';
 * import { defineComponent } from '@shared/ecs';
 *
 * @module
 */

export type * from './types';
export * from './utils';
export * from './ecs';
